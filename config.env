# Shared project configuration
# Each tool owns its own config: terraform.tfvars, ansible/group_vars/all.yml, helm/values-*.yaml
# This file only holds values shared across multiple tools (used by: Makefile, docker-compose.yml)
# Secrets go in .env.secrets (git-ignored, generated via: make generate-secrets)

PROJECT_NAME=devboard
DOMAIN=devboard.local
DOCKER_REGISTRY=ghcr.io/waddenn/projet-etude
SERVER_IP=192.168.1.40
